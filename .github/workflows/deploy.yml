name: Deploy Webapp

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, deploy]
    steps:
      - name: Deploy webapp
        run: |
          cd /home/isaac/here.news/webapp
          git pull
          ./scripts/deploy.sh
